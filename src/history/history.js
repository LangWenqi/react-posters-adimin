// import createHistory from 'history/createBrowserHistory';//创建BrowserRouter
import createHistory from 'history/createHashHistory';//创建HashRouter
const history = createHistory();
export default history;
